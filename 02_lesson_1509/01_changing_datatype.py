# x = 5 - хардкод

num = int( input('Введи число: ') )  # эта переменная НЕ МОЖЕТ принимать ни дробные числа, ни строки
print(num / 6)
'''
функция input(text) берет значение с клавиатуры
и записывает его в переменную. Текст, написанный в скобках
отображается пользователю.

input ВСЕГДА возвращает строчку, поэтому тип данных 
надо обязательно приводить
'''

